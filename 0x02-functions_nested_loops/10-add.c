#include "main.h"
#include <stdio.h>

/**
 * add - make sum of two number
 * @c : is int
 * @a: int number two
 * Return: int is the last digit of @c;
 **/
int add(int c, int a)
{

	return (c + a);
}
